---
layout: ../layouts/GistLayout.astro
tags: [bash]
---

# Bash - echo for base64 encoding

Bash - echo for base64 encoding

Always use `-n` flag to suppress the newline that echo adds otherwise.

`echo -n <SOME_STRING>`
